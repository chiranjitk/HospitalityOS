{
  "name": "staysuite-captive-redirect",
  "version": "3.0.0",
  "description": "StaySuite Captive Portal HTTP Redirect Server v3.0 — high-performance captive portal detection and redirection with HTTPS support, 5k+ concurrent sessions, device-agnostic compatibility, rate limiting, and real-time metrics",
  "main": "index.ts",
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts"
  },
  "dependencies": {}
}

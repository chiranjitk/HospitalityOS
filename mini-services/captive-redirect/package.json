{
  "name": "staysuite-captive-redirect",
  "version": "1.0.0",
  "description": "StaySuite Captive Portal HTTP Redirect Server — intercepts unauthenticated guest traffic and redirects to login portal",
  "main": "index.ts",
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts"
  }
}

/**
 * Test Setup
 * 
 * Ensures Prisma can connect and sets up global test utilities.
 * vitest.config.ts already loads .env for DATABASE_URL before Prisma initializes.
 */
console.log('[setup] Test setup running...');
